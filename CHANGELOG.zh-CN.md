# 更新日志

## v2.0.5 - 2026-07-26

### 重构

- 插件统一更名为 `Pynseq for Weibo｜屏序·微博`。
- 设置页面全面采用 Pynseq for Douyin 的复古视觉系统，并按「常规、屏蔽设置、本地屏蔽名单、新浪微博黑名单管理、关于」重新组织微博自身功能。
- 新增五步设置向导，覆盖浏览体验、快捷入口、操作确认、五类屏蔽范围，以及 GitHub 和 Buy Me a Coffee 支持入口。
- Tampermonkey 菜单精简为「设置」和「关于」。
- 移除高级诊断页面及对应运行逻辑。
- 本地屏蔽名单导出文件改为 `pynseq-for-weibo-blocklist-YYYY-MM-DDTHH-mm-ss.json`。
- GitHub 项目地址统一更新为 `DanielZenFlow/Pynseq-Weibo`。
- 设置向导右上角入口改为「使用默认设置」，五个设置分类使用更醒目的粗体字重。
- 确认对话框与成功提示统一对齐 Pynseq for Douyin 的尺寸、方角、米色背景和珊瑚色操作按钮。
- 「常规」选项卡中的设置向导入口移动到页面最上方。
- README 参考 Pynseq for Douyin 重写为中英双语结构，并补充 Greasy Fork、GitHub 与 Buy Me a Coffee 项目入口。
- 用户脚本文件更名为 `pynseq-for-weibo.user.js`，并加入采用 Daniel Zenflow 标志的非透明油猴图标。

### 修复

- 修复「全部关注」页右键屏蔽后只隐藏作者昵称区域的问题；现在会隐藏并压缩整条微博容器，让下一条微博立即补位。
- 修复关闭某类屏蔽范围后，之前被误判并隐藏的容器无法合理恢复的问题。
- 修复新浪微博黑名单同步结果提示被设置面板遮挡的问题，提示层现在始终位于设置页面上方。
- 修复已停留在「全部关注」页时，从设置向导启用「最新微博」只更新配置和样式、却不切换时间线的问题；向导和普通保存现在共用同一套即时切换逻辑。
- 加固配置存储与微博接口响应的对象键处理，拒绝可能触发原型污染的危险键。

## v2.0.0 - 2026-07-24

### 新增

- 新增默认开启的“屏蔽用户前确认”选项，加入本地屏蔽列表和同时加入新浪微博官方黑名单均会在执行前显示确认。
- 外观设置新增「显示右下角设置按钮」开关；关闭后仍可通过 Tampermonkey 菜单中的「打开脚本设置」进入设置。
- 新增独立「本地屏蔽列表」页面，直接读取全部已保存 UID，支持搜索、输入页数快速跳转、分页浏览、批量新增和单条删除。
- UID 列表每页显示 50 条，最近写入的 UID 优先，并提供用户主页快捷入口。
- 设置内新增独立的「高级诊断」页面，可查看配置模式版本、本地屏蔽数量、官方同步状态、中继状态、DOM 调度统计、虚拟列表统计及浏览器能力；页面位置仅记录分类而非原始路径，诊断信息不包含 UID 列表，也不会自动上传。

### 重构

- 设置面板改为标准软件式布局，使用「常规、外观、屏蔽设置、本地屏蔽列表、数据管理、高级诊断」六个侧栏页面，顶部标题和底部保存操作保持固定。
- 重新组织原有设置项、本地屏蔽列表和新浪微博官方黑名单同步工具，增加响应式窄屏布局、深色外观适配、键盘方向键切换分类及 `Esc` 关闭支持。
- 右下角设置入口改为圆形齿轮图标，并增加悬停旋转、层次阴影和深色模式适配。
- 主站及 `s.weibo.com` 搜索首页存在新微博时，顶部首页按钮的红色 `NEW` 文字徽标显示为红色圆点。
- 本地屏蔽列表备份文件中的版本标记更新为 `2.0.0`。
- 新增可重复运行的逻辑回归测试，覆盖广告与本地屏蔽规则联合过滤、接口范围识别和响应重建。
- 配置存储增加模式版本和集中读写入口，旧版合并导航设置会自动迁移；无法解析的配置会先保存恢复备份，再使用默认配置。
- 页面内容过滤、搜索结果过滤和侧栏处理共用一个 DOM 变化观察器、路由事件中心及命名任务调度器，减少重复扫描与多层历史记录包装。
- 虚拟列表压缩状态收敛到独立运行时控制器，并在路由变化或恢复隐藏内容时统一重置。

### 修复

- 脚本中的屏蔽、UID 删除、替换导入、清空本地屏蔽列表、首次同步和 Star 确认统一显示在屏幕中央，并避免多个确认框同时重叠。
- 配置写入成功后会立即关闭设置面板，后续即时应用异常不会阻止关闭；保存结果仍通过页面顶部的非阻塞通知显示。
- 关闭「主页默认显示最新微博」后会明确切换到微博原生「全部关注」标签，并恢复对应入口和接口行为，不再依赖微博记忆的上一次标签。
- 本地屏蔽列表中的删除会记录为排除项，启动时的自动同步不会再次加入；手动同步仍可重新导入新浪微博官方黑名单中的 UID。
- 完整同步、增量同步和分页同步遇到限流或 HTTP 错误时会明确报告失败，不再把部分结果显示为同步成功。
- 完整同步和分页同步会检测重复游标，完整同步同时设置 5000 页安全上限，避免异常响应造成无限请求。
- 修复嵌套回复包含本地屏蔽用户时误删上层正常评论的问题，并让微博、评论、搜索结果、用户卡片和互动列表开关分别控制对应数据。
- 本地屏蔽列表缓存与排除项支持跨标签页更新；同步结束时会合并同步期间发生的本地删除，避免重新写回已删除 UID。
- 新浪微博官方黑名单主站中继改为按请求隔离，同时发起多个屏蔽操作时不会互相覆盖。
- 新浪微博官方黑名单主站中继优先通过跨标签页值变更事件接收结果，并保留超时、资源清理和旧脚本管理器兼容路径；官方黑名单分页请求仍固定等待 350ms。
- 配置变更支持跨标签页即时更新页面过滤、侧栏和设置入口。
- 视频、推荐和游戏的隐藏选择器限定在导航容器内，不再影响正文中的普通链接。
- 右键用户菜单将“在新选项卡中打开链接”调整为第一项，并使用分割线与两个屏蔽操作分组。
- “关于”改用脚本内对话框显示，GitHub 地址显示为简短的项目主页链接，滚动条轨道与对话框背景保持一致。
- 设置面板统一降低主窗口、分区、按钮、输入框和列表的圆角。
- 设置面板与运行层改为脚本私有闭包通信，不再向微博页面暴露官方黑名单同步和确认对话框接口。
- 新浪微博官方黑名单同步增加单任务互斥、页数与处理量进度、按钮运行状态和取消能力。
- 网络拦截限定为已知微博域名和接口路径；修改 JSON 响应时保留原响应状态及元数据，并移除失效的内容长度和压缩标记。
- 广告与本地屏蔽响应过滤合并为一次递归遍历；未命中过滤条件时直接保留原响应数据。
- 本地屏蔽列表、设置面板、右键菜单、提示消息和确认对话框不再被内容过滤器隐藏。
- 设置保存改为原地更新运行时配置、页面样式、侧栏内容和右下角入口；仅在首页时间线模式发生变化时执行必要的路由跳转。
- 操作结果与错误反馈统一改为可自动关闭的非阻塞通知，不再使用浏览器原生提示框。
- 热搜及侧栏模块改为可恢复隐藏，关闭对应设置后可直接恢复显示。
- 修复页面滚动时右键用户菜单不会关闭的问题。
- Star 提醒现在按首次、7 天、30 天和 90 天依次推进，完成后停止提醒。
- 首次运行的同步提示与 Star 提醒不再连续弹出。
- 移除每 600 毫秒执行一次的侧栏布局轮询，改为通过页面变化、滚动、窗口尺寸和可见性事件刷新。

## v1.2.3 - 2026-07-24

### 新增

- 设置面板新增黑名单内容管理，可分别控制微博/转发、评论/回复、搜索结果、用户卡片/推荐项，以及转发/点赞用户列表；关注和粉丝关系页继续保留显示。
- 设置面板新增「隐藏搜索页相关用户」选项，默认隐藏 `s.weibo.com` 综合搜索页右侧的「相关用户」整个卡片。
- 新增可明确识别的广告过滤及设置开关，同时识别接口中的广告字段和页面中的「广告 / 推广 / 赞助」标识。

### 改进

- 右键用户菜单现在只在用户名文字区域触发；头像、正文、操作按钮及卡片空白处使用浏览器原生右键菜单。

### 修复

- 修复启用「主页默认显示最新微博」后，首页/分组时间线的用户名采用嵌套 `<span title>` 结构而无法触发右键菜单的问题。
- 修复关闭「主页默认显示最新微博」后仍停留在 `/mygroups` 最新微博页、看起来与开启时没有区别的问题；在首页时间线路由切换该选项后会回到微博首页重新应用设置。
- 「全部关注」接口过滤现在受「主页默认显示最新微博」开关控制；关闭后恢复微博原生首页接口行为。
- 修复 `s.weibo.com` 刷新后通用内容根扫描仍可能漏过搜索卡片的问题；新增搜索页专用过滤器，只读取卡片头部主作者 UID，并通过独立观察器、DOM 就绪、页面加载完成和延迟队列持续补扫。
- 补充 `usercard="id=UID"`、`data-user-card` 等复合属性中的 UID 识别。
- 修复搜索结果卡片右键时误从“转发/评论”操作区读取 UID 和文案的问题；现在优先识别点击位置对应的用户，并在头像缺少 UID 时回退到卡片主作者。
- 修复右键菜单“同时加入新浪微博黑名单”仍使用表单编码、被微博接口拒绝的问题，改为当前接口使用的最小 JSON 请求格式并严格校验成功响应。
- 微博 WAF 会拒绝搜索子域发起的跨域拉黑请求；现在会自动打开一个非活动主站中继页，以同源请求加入新浪微博黑名单，并在完成后自动关闭。

### 兼容性

- 新增 `data-user-card` 与 `data-usercard-mid` 的 UID 识别支持。

## v1.1.3 - 2026-05-21

### 新增

- 新增 `s.weibo.com` 搜索页匹配规则，脚本现在会在微博搜索页运行。
- 新增搜索结果 DOM 黑名单过滤，会从 `data-user-id`、`data-uid`、`uid`、`usercard`、`/u/UID`、`/p/100505UID` 等结构中提取 UID，并隐藏命中的结果卡片。
- 新增微博搜索页热搜容器清理，覆盖 `#hot-band-container`、`.hot-band-container` 和 `.hot-band-tabs`，避免只隐藏内容但留下外层空容器。
- 导航栏入口设置拆分为三个独立选项：隐藏「视频」、隐藏「推荐」、隐藏「游戏」。

### 改进

- 兼容旧版 `hideNavVideoRecommend` 配置；旧用户开启过原合并开关时，会自动迁移为「视频」和「推荐」都隐藏。
- 搜索页只使用本地黑名单缓存，不再尝试调用主站黑名单同步接口。
- 在非主站页面手动同步黑名单时，会提示回到 `weibo.com` 主站执行。
- `fetch` 拦截兼容字符串、`URL` 和 `Request` 三种输入形式。
- 控制台启动日志现在输出真实脚本版本和当前域名，便于确认脚本是否在目标页面运行。

### 修复

- 修复「游戏」导航入口未被隐藏的问题，增加 `title="游戏"` 和 `game.weibo.com` 链接匹配。
- 修复微博搜索页黑名单过滤不生效的问题。
- 修复微博搜索页热搜列表隐藏后仍残留标题、刷新按钮和底部链接容器的问题。

### 注意事项

- 搜索页过滤依赖本地缓存的黑名单 UID；请先在 `weibo.com` 主站完成黑名单同步。
- 更新油猴脚本时，如果版本号保持 `1.1.3`，浏览器可能不会自动刷新已安装脚本，需要手动复制最新内容并保存。
